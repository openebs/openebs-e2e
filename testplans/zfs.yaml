meta:
  environment: {}
  name: 'zfs'
  description: 'zfs e2e tests'
  include:
    - common
testsuites:
  - zfs_volume_provisioning
  - zfs_volume_snapshot
  - zfs_custom_node_topology
  # - zfs_ha_controller // this test require minimum two worker nodes in cluster
